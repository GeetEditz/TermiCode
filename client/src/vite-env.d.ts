/// <reference types="vite/client" />

interface ImportMeta {
    readonly env: {
      readonly VITE_API_URL: "https://code-editor-server-production.up.railway.app";
      // Add other environment variables as needed
    };
  }